@model PedidoViewModel
@{
    ViewData["Title"] = $"Editar pedido #{Model.Id}";
}

<div class="page-head">
    <h1 class="page-title"><i class="bi bi-receipt"></i>Editar pedido #@Model.Id</h1>
    <a asp-action="Index" class="btn btn-outline-secondary"><i class="bi bi-arrow-left me-1"></i>Volver a la lista</a>
</div>

<form asp-action="Edit" asp-route-id="@Model.Id" id="pedidoForm">
    <partial name="_PedidoCampos" model="Model" />
</form>

@section Scripts {
    <script src="~/js/pedido.js" asp-append-version="true"></script>
}
